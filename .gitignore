# Bỏ qua tất cả các file và thư mục ở cấp cao nhất (root)
/*

# Ngoại trừ 2 thư mục frontend và backend
!/frontend/
!/backend/

# Ngoại trừ chính file .gitignore này
!/.gitignore

# (Tuỳ chọn) Ngoại trừ thư mục cấu hình Github
!/.github/

# --- CÁC CẤU HÌNH BỎ QUA RIÊNG ---
# Bỏ qua các thư mục/file rác không muốn đưa lên git (áp dụng cho cả backend và frontend)
skills/
postman/
*.postman_collection.json