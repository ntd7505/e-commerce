# Ignore everything at the repository root by default.
/*

# Keep the application workspaces tracked.
!/frontend/
!/backend/

# Keep repository-level configuration tracked.
!/.gitignore
!/.github/

# Root-only local files and exported artifacts.
/skills/
/postman/
/*.postman_collection.json
